[package]
name = "rle-idl"
version = "0.1.0"
edition = "2021"
description = "IDL interface for the RLE compression demo spier"
license = "MIT"

build = "build.rs"

[dependencies]
dynspire = { path = "../../dynspire" }

[build-dependencies]
dynspire-codegen = { path = "../../dynspire-codegen" }
