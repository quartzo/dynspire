[package]
name = "rle-host"
version = "0.1.0"
edition = "2021"
description = "Host binary — loads librle_spier.so at runtime and calls all IDL methods"
license = "MIT"

[[bin]]
name = "rle-host"
path = "src/main.rs"

[dependencies]
rle-idl = { path = "../rle-idl" }
